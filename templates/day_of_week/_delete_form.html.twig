<form method="post" action="{{ path('day_of_week_delete', {'id': day_of_week.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day_of_week.id) }}">
    <button class="btn">Delete</button>
</form>
