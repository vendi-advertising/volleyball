{% extends 'base.html.twig' %}

{% block title %}DayOfWeek{% endblock %}

{% block body %}
    <h1>DayOfWeek</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ day_of_week.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ day_of_week.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('day_of_week_index') }}">back to list</a>

    <a href="{{ path('day_of_week_edit', {'id': day_of_week.id}) }}">edit</a>

    {{ include('day_of_week/_delete_form.html.twig') }}
{% endblock %}
